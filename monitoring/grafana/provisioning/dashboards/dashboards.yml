# Grafana Dashboard Provisioning Configuration
apiVersion: 1

providers:
  - name: 'F1PA Dashboards'
    orgId: 1
    folder: 'F1PA'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
